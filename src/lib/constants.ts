/** Height of the sticky navigation bar in pixels. */
export const NAV_HEIGHT_PX = 60;

/** Maximum length for bucket list item labels. */
export const MAX_ITEM_LABEL_LENGTH = 200;

/** Maximum length for bucket list item info text. */
export const MAX_ITEM_INFO_LENGTH = 500;

/** Maximum number of bucket list items. */
export const MAX_BUCKET_LIST_ITEMS = 100;
